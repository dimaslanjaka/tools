domain: www.webmanajemen.com       # if you want to force HTTPS, specify the domain without the http at the start, e.g. example.com
url: https://www.webmanajemen.com  # the base hostname and protocol for your site, e.g. http://example.com
baseurl: /tools/      # place folder name if the site is served in a subfolder
theme: jekyll-theme-midnight
plugins:
  - jekyll-readme-index
